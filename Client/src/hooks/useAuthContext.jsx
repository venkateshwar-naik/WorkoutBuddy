import {AuthContext} from"../context/Autentication";

import {  useContext } from "react";
export const useAuthContext =()=>{
    const context= useContext(AuthContext);
    if(!context){
        throw Error("useAuthContext cant be used")
    }
    return context
}























































































































